spring:
  application:
    name: BuDongSan

  datasource:
    url: jdbc:mysql://localhost:3306/housing_mysql
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    mongodb:
      uri: mongodb://root:1234@localhost:27018/housing?authSource=admin # MongoDB 연결 추가

    redis:
      host: localhost
      port: 6384

server:
  port: 8080
